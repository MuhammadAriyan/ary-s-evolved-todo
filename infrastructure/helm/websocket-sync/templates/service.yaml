apiVersion: v1
kind: Service
metadata:
  name: {{ include "websocket-sync.fullname" . }}
  labels:
    {{- include "websocket-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "websocket-sync.selectorLabels" . | nindent 4 }}
