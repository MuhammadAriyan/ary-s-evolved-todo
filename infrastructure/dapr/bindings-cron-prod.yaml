apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: cron-reminder-check
  namespace: default
spec:
  type: bindings.cron
  version: v1
  metadata:
    - name: schedule
      value: "*/1 * * * *"  # Every minute
    - name: direction
      value: "input"
